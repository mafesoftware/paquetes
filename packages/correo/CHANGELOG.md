# Changelog

## 0.1.0

Envío de mails transaccionales por Resend: `enviarCorreo` (resultados en vez
de excepciones, `fetch` inyectable), `plantillaCorreo` con la marca de cada
producto, `botonCorreo` y `escapeHtml`.
