{
  "name": "@mafesoftware/correo",
  "version": "0.1.0",
  "description": "Envío de mails transaccionales por Resend para los productos de MAFE Software. Sin dependencias, sin framework: fetch inyectable, resultados en vez de excepciones y una plantilla HTML con la marca de cada producto.",
  "license": "MIT",
  "type": "module",
  "files": ["dist"],
  "exports": {
    ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5",
    "vitest": "^3.2.4"
  },
  "engines": { "node": ">=20" },
  "keywords": ["resend", "email", "transaccional", "argentina"],
  "publishConfig": { "access": "public" }
}
